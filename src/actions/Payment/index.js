import { receiveProducts } from "..";
import { get, post } from "../../api/APIController";
import store from "../../Store";
export const getStripeToken = (params) => {
  return (dispatch) => {
    post("stripe-token", params)
      .then((response) => {
        if (response.status == 200) {
        }
      })
      .catch()
      .finally();
  };
};

export const getStripeToken = (params) => {
  return (dispatch) => {
    post("stripe-token", params)
      .then((response) => {
        if (response.status == 200) {
        }
      })
      .catch()
      .finally();
  };
};

export const getStripeToken = (params) => {
  return (dispatch) => {
    post("stripe-token", params)
      .then((response) => {
        if (response.status == 200) {
        }
      })
      .catch()
      .finally();
  };
};

export const getStripeToken = (params) => {
  return (dispatch) => {
    post("stripe-token", params)
      .then((response) => {
        if (response.status == 200) {
        }
      })
      .catch()
      .finally();
  };
};
